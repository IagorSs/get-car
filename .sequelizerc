const path = require("path");

module.exports = {
  config: path.resolve(__dirname, "src", "config", "database", "dbData.js"),
  "migrations-path": path.resolve(
    __dirname,
    "src",
    "config",
    "database",
    "migrations"
  ),
  "models-path": path.resolve(__dirname, "src", "models"),
  "seeders-path": path.resolve(
    __dirname,
    "src",
    "config",
    "database",
    "seeders"
  ),
};
